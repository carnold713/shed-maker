# Rules index

_Auto-generated by `scripts/gen-rules-index.ts` — do not edit by hand._

42 rules registered in `rules/index.ts`.

| Id | Title | Source | Rationale |
|---|---|---|---|
| `structural.site.frostDepthVerified` | Frost depth set and verified | `IRC:R403.1.4.1` | Footings and embedded posts must extend below the frost line. Frost depth varies by jurisdiction; verify locally. |
| `structural.roof.clearSpanLimit` | Truss clear span within common prescriptive range | `NFBA:Post-Frame Building Design Manual` | Wood trusses over 60' clear span are routinely built but require engineered design and manufacturer review; flag it early. |
| `design.openings.fitsWall` | Opening fits its wall | `Industry` | An opening past the end of a wall or above the eave cannot exist. |
| `framing.openings.overlap` | Openings do not overlap | `Industry` | Overlapping rough openings cannot both be framed. |
| `structural.openings.overheadHeadroom` | Overhead door headroom | `Industry` | A sectional door needs roughly 12" above the opening for the track and springs, a roll-up coil about 18"; the trusses bear at the eave, so the door head plus headroom must stay below it. |
| `framing.openings.cornerClearance` | Opening clear of corners | `NFBA:Post-Frame Building Design Manual` | A corner post plus jamb framing and corner trim need about 12" of wall; an opening tighter than that cannot be framed cleanly. |
| `framing.openings.postLine` | Small opening crosses a post line | `NFBA:Post-Frame Building Design Manual` | Post spacing is the structural module; a man door or window that lands on a post forces the post to move and a header to carry the girts. Centre it in a bay instead. |
| `design.zones.insideFootprint` | Zones inside the footprint | `Industry` | A pen past the exterior wall means the building needs another bay or a wider truss span. |
| `design.zones.overlap` | Zones do not overlap | `Industry` | Two pens or rooms cannot occupy the same floor; the partition between them has nowhere to go. |
| `animals.pen.minimumSize` | Pen meets species minimum | `Species:extension guidelines (see rules/animals/presets.ts)` | Below-minimum pens stress animals and are hard to clean; recommended sizes are larger still. |
| `design.aisle.minWidth` | Aisle wide enough | `Species:extension-equine (12' min, 14' recommended for horses)` | Aisles must pass a horse being led beside a cart or a small tractor; narrow aisles are the most common regret. |
| `design.pen.access` | Pen has a door to an aisle or outside | `Industry` | A pen you cannot reach without walking through another pen cannot be mucked, fed, or evacuated. |
| `framing.partitions.onPostLine` | Stall walls land on post lines | `NFBA:Post-Frame Building Design Manual` | A stall wall on a post line can be built between the posts with T&G in channels; off the grid it becomes a stick-framed partition. |
| `design.leanTo.clearance` | Lean-to low edge clears 7' | `Industry` | At 3:12 a 12' lean-to drops 3'; with a 6" drop below a 10' eave the outer edge lands at 6'6" — too low for a run-in. Raise the eave, flatten the pitch, or shorten the depth. |
| `structural.leanTo.rafterSpan` | Lean-to rafter span | `IRC:R802.4 (2×6 SPF #2 rafters, 24" OC)` | Beyond about 12' of projection 2×6 rafters at 24" run out of table; use 2×8/2×10 rafters or trusses and have the truss supplier confirm. |
| `design.interiorDoor.width` | Stall door width | `Species:extension-equine (stall door 4' min for horses)` | A horse needs a 4' clear stall door; goats and sheep 3'. Narrow doors catch hips and gates. Cased openings into hay bays should pass a bale cart (4'). |
| `design.interiorDoor.exists` | Every stall and room has a door | `Industry` | Pens need a stall door to the aisle (or outside access); rooms need a door. A zone whose doors were all deleted, or that touches nothing but other pens, has no way in. |
| `mep.electrical.panel` | Panel placed | `NEC:408 / 110.26` | Circuits are routed from the panel, so its position sets every wire length. Keep a 30" wide × 36" deep × 6'6" high working space in front of it (110.26(A)). |
| `mep.electrical.panelWorkspace` | Panel working space | `NEC:110.26(A)` | A panel inside a stall is unreachable, gets kicked, and has no clear working space. Put it in an aisle, utility or tack room. |
| `mep.electrical.serviceCapacity` | Service size vs demand | `NEC:220 / 215.2` | The demand load (lighting and heaters at 125 %, receptacles at 180 VA each, largest motor +25 %) has to fit the feeder and sub-panel with room to grow. Above 80 % pick the next size. |
| `mep.electrical.lightingLevel` | Light levels by area | `ASABE:EP344.4 (lighting for agricultural facilities)` | Targets: stalls 10 fc, aisles 20 fc, tack / feed / wash 30 fc, office and milking 50 fc, measured at the floor. Sized by the lumen method with a 0.5 utilisation factor for bare wood and steel. |
| `mep.electrical.luminaireProtection` | Guarded fixtures in stalls | `NEC:547.8 (2023: 547.31)` | Luminaires in livestock areas must keep dust out, be protected from physical damage, and be watertight where washed down. A rearing horse reaches 11': mount stall lights at 11' or fit a wire guard, and never below 8'. |
| `mep.electrical.animalReach` | Devices out of animal reach | `NEC:547.5 physical protection (2023: 547.26)` | Receptacles and switches belong on the aisle side of the stall front at 48", never inside the pen — on its outside wall or in the open — where they get chewed, kicked and soaked. Waterers inside the pen are hard-wired in conduit. |
| `mep.electrical.heaterPlacement` | No unit heaters in stalls | `Industry` | Electric unit heaters belong in tack, wash and office rooms, well above bedding; in a stall they are a fire and burn hazard. |
| `mep.electrical.switchAtEntry` | Switch at every walk-in door | `MWPS:MWPS-1 (farm buildings wiring) / NEC:210.70(C)` | You should never cross a dark barn to find the lights: put a switch just inside every man door and at both ends of a long aisle. |
| `mep.electrical.voltageDrop` | Voltage drop under 3 % | `NEC:210.19(A) IN 4 / 215.2(A) IN 2` | Long barn runs at 120 V lose voltage in the wire; over 3 % lights dim and motors run hot. The derivation upsizes the conductor until the run is under 3 %. |
| `mep.electrical.agriculturalWiring` | Agricultural wiring & GFCI | `NEC:547.5(A),(C),(G) (2023: 547.26, 547.28)` | Livestock buildings are damp, dusty and corrosive: use UF or NMC cable in PVC conduit (or jacketed MC), corrosion-resistant PVC boxes, and GFCI protection on every 120 V receptacle. |
| `mep.electrical.equipotential` | Equipotential plane in concrete stalls | `NEC:547.10 (2023: 547.44)` | Stray voltage across a concrete floor with metal stall fronts or waterers is felt by animals before people. Bond wire mesh or rebar in the slab under livestock areas to the grounding system. |
| `mep.drainage.washBay` | Wash bay has a drain | `MWPS:MWPS-1 (wash rack ¼"/ft to a drain) / IPC:412` | A wash bay without a drain sends water down the aisle and into the stalls. A trench drain across the bay, or a floor drain in the middle with the slab sloped ¼" per foot to it, keeps the floor dry. |
| `mep.drainage.outlet` | Drains have an outlet | `IPC:704.1 / 708.1` | Every drain needs a pipe that falls at least ⅛" per foot to somewhere — daylight on the downhill side, a dry well, or a tank. Without an outlet the concrete crew has nothing to sleeve and the plumber nothing to connect. |
| `mep.drainage.daylight` | Outlet can daylight | `IPC:704.1 (fall) / Industry` | The pipe leaves the slab about a foot down and drops ⅛"–¼" every foot of run. To end on open ground its crown must clear the surface, so the ground at the outlet has to be lower than the pipe by that much. Otherwise run it further downhill, raise the pad, or use a dry well. |
| `mep.drainage.inStall` | No drains in stalls | `Species:extension-equine (stall flooring: flat over mats, drain the aisle)` | Bedding and manure clog a stall drain within weeks and a horse will dig at it. Keep stalls flat over mats and slope the aisle or wash bay to the drain instead. |
| `mep.drainage.slopeRun` | Slab slope run to a drain | `MWPS:MWPS-1 / Industry` | At ⅛" per foot a drain 25' away means a 3" high point — a slab that noticeably tilts and a long way for water to travel. Keep any point that drains within about 25' of its drain; add a second drain or a trench for bigger areas. |
| `mep.drainage.traps` | Traps, primers and where wash water goes | `IPC:1002.1 / 1002.4 / 708.1` | Every floor drain needs a trap so sewer or soil gas stays out, and a deep seal or trap primer so it doesn't dry out between washes. Wash water carries manure: many counties refuse it in storm drains and septic tanks need an interceptor — ask before the pipe goes in. |
| `site.run.building` | Run is outside the walls | `Industry` | A run is fenced ground outside the barn; one drawn over the building would put fence posts through the slab. |
| `site.run.overlap` | Runs don't overlap each other | `Industry` | Two runs that overlap share ground the fence takeoff counts twice; runs side by side share one fence line instead. |
| `site.run.space` | Run gives each animal enough ground | `Species:extension dry-lot guidance (docs/research/runs-and-fencing.md §2)` | A run that is too small turns to mud and manure in a season and the animals stand in it. Extension dry-lot figures are per head; a horse wants at least a 12' × 24' run off its stall and 400–1,000 sq ft in a lot. |
| `site.run.fenceHeight` | Fence is tall enough for the animal | `Species:extension fencing guidance (docs/research/runs-and-fencing.md §3)` | Horses go over 4' fences, goats over anything under 4', and a 6' fence is what keeps a dog in and a coyote out of a chicken run. |
| `site.run.fenceKind` | Fence type suits the animal | `Species:extension fencing guidance (docs/research/runs-and-fencing.md §3)` | Field fence with 6" openings catches a horse's hoof or a goat's head; poultry netting stops nothing bigger than a hen; electric alone does not hold small animals or keep predators out. |
| `site.run.penDoor` | Run opens from its stall | `Industry` | The point of an attached run is that the stall's outside door opens into it, so the animal comes and goes without being led. A run on a pen with no outside door is a fenced lawn. |
| `site.run.gate` | Run has a gate | `Industry` | You need a way in from outside for the wheelbarrow, the vet and the farrier that doesn't go through the stall. A 4' walk gate is the minimum; a 12' gate lets a tractor in to scrape the lot. |
| `design.footprint.module` | Footprint on 2' module | `Industry` | Sheet goods are 4×8 and trusses/purlins lay out on 2' centers; dimensions off the 2' module add cut waste. |
